services:
  mariadb:
    image: mariadb
    container_name: mariadb
    build: ./requirements/mariadb
    restart: always
    networks:
      - inception-net
    volumes:
      - ~/data/db_files:/var/lib/mysql
    env_file:
      - .env

  wordpress:
    image: wordpress
    build: ./requirements/wordpress
    container_name: wordpress
    restart: always
    depends_on:
      - mariadb
    volumes:
      - ~/data/wp_files:/var/www/inception
    networks:
      - inception-net
    env_file:
      - .env

  nginx:
    image: nginx
    build:
      context: ./requirements/nginx/
      args:
        SSL_PATH: ${SSL_PATH}
        SSL_CERT: ${SSL_CERT}
        SSL_KEY: ${SSL_KEY}
        COUNTRY: ${COUNTRY}
        STATE: ${STATE}
        LOCALITY: ${LOCALITY}
        ORGANIZATION: ${ORGANIZATION}
        UNIT: ${UNIT}
        COMMON_NAME: ${COMMON_NAME}
    container_name: nginx
    restart: always
    ports:
      - "443:443"
    depends_on:
      - wordpress
    volumes:
      - ~/data/wp_files:/var/www/inception
    networks:
      - inception-net
    env_file:
      - .env

networks:
  inception-net:
    driver: bridge
